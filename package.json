{
  "name": "mobile-models-api",
  "version": "1.0.0",
  "description": "MobileModels Device Database API - Cloudflare Worker",
  "main": "src/worker.js",
  "scripts": {
    "build": "node scripts/build-db.js",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
